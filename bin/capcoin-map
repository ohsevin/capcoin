#!/usr/bin/env node
var program = require('commander');

program
  .description('Check global stats')
  .option('--json', 'only JSON output')
  .usage('[options]')

var cli = require('../cli')(program);

cli
  .commands
  .map()
  .then(cli.helpers.horizontalTable)
  .catch(console.error)
